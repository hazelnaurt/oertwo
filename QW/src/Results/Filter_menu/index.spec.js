import React from "react";
import Filter_menu from ".";
import { shallow } from "enzyme";
import { expect } from "@jest/globals";

describe("Filter_menu component", () => {
  test("Test1", () => {
    
  });
});
