import React from "react";
import Detail_item from ".";
import { shallow } from "enzyme";
import { expect } from "@jest/globals";

describe("Detail_item component", () => {
  test("Test1", () => {
    
  });
});
